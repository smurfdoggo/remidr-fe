# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# NativeWind

- This project uses NativeWind 4.2.7 with Tailwind CSS 3.4.17.
- Before changing NativeWind configuration, styling behavior, or NativeWind-specific APIs, search `docs/ai/nativewind-v4.2.7-llms-full.txt` and read only the relevant sections.
- The snapshot may contain NativeWind v5 preview documentation. Do not apply v5 guidance unless the task explicitly requests a v5 upgrade.
- Treat `package.json`, `package-lock.json`, and the installed package behavior as authoritative when they conflict with the documentation snapshot.
